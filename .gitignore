# Gradle
.gradle/
build/
out/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws

# Env — ignore every dotenv variant; .env.example is the only tracked one.
.env
.env.*
!.env.example

# Local working-state doc — not part of the shipped reference.
PLAN.md

# Native SDK lib staged for `docker build` — see README "Deploy the server".
# 50MB binary; build it per-host with cross or the Rust toolchain, don't commit.
libs/

# OS
.DS_Store
Thumbs.db

# Logs / scratch
*.log
logs/

# Node (client/, added in Phase 3)
node_modules/
.next/
.vercel/

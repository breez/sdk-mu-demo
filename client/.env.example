# Server's public URL. In Vercel set NEXT_PUBLIC_API_BASE_URL via the
# project settings UI (or `vercel env add`). Locally, copy this file to
# `.env.local`.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
